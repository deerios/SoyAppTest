{
  "$schema": "https://developers.reddit.com/schema/config-file.v1.json",
  "name": "soyapptest",
  "server": {},
  "menu": {
    "items": [
      {
        "label": "[soyapptest] Sync Queue Now",
        "description": "Check the video queue and post any new videos immediately.",
        "location": "subreddit",
        "endpoint": "/internal/on/menu/new-post"
      }
    ]
  },
  "triggers": {
    "onAppInstall": "/internal/on/app/install"
  },
  "scheduler": {
    "tasks": {
      "post-video": {
        "endpoint": "/internal/on/schedule/post-video",
        "cron": "*/5 * * * *"
      }
    }
  },
  "permissions": {
    "http": {
      "enable": true,
      "domains": ["discord.com"]
    }
  },
  "settings": {
    "global": {
      "queueChannelId": {
        "type": "string",
        "label": "Queue Channel ID",
        "helpText": "Discord channel ID the soybot pipeline posts queue entries to."
      },
      "discordBotToken": {
        "type": "string",
        "label": "Discord Bot Token",
        "helpText": "Bot token used to read the queue channel via Discord's REST API.",
        "isSecret": true
      }
    }
  },
  "scripts": {
    "dev": "npm run watch"
  },
  "dev": {
    "subreddit": "soyapptest_dev"
  }
}
